description: >
  This command ensures that all required dependencies for the orb's operations are in place.
# What will this command do?
# Descriptions should be short, simple, and clear.

steps:
  - run:
      name: Setup Dependencies
      command: <<include(scripts/setup_dependencies.sh)>>
